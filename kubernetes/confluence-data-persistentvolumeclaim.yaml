apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: confluence-data
  name: confluence-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
